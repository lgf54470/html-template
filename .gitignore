.freebuff/
# 本地数据库(runtime 数据,不入库;首次启动自动创建)
sqlite.db
sqlite.db-wal
sqlite.db-shm

# 本地环境变量(不入库;模板见 .env.example)
.env

# wrangler 本地调试环境变量(不入库;模板见 .dev.vars.example)
.dev.vars

# 本地加密密钥(不入库;生产环境改用 ENCRYPTION_KEY 环境变量)
server/.secret-key
