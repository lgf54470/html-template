/* ============================================================
 * expiry.js — 会话失效时长(纯常量,Node / Worker 共用)
 * ------------------------------------------------------------
 * 'browser' = 下一次浏览器打开;服务端给 30 天上限兜底,
 * 真正的"关浏览器即失效"由客户端 sessionStorage 保证。
 * ============================================================ */
'use strict';

const EXPIRY_MS = {
  '3h': 3 * 3600e3,
  '6h': 6 * 3600e3,
  '9h': 9 * 3600e3,
  '12h': 12 * 3600e3,
  '24h': 24 * 3600e3,
  '7d': 7 * 86400e3,
  '14d': 14 * 86400e3,
  '30d': 30 * 86400e3,
  browser: 30 * 86400e3,
};

module.exports = { EXPIRY_MS };
